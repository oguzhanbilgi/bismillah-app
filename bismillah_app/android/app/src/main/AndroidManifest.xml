<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Namaz vakti hesabı için ön-plan konumu (TASK 021). SADECE foreground:
         ACCESS_BACKGROUND_LOCATION KASITLI OLARAK YOKTUR; arka plan/sürekli
         takip yapılmaz, koordinat Firebase'e/analytics'e gitmez. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Namaz vakti hatırlatıcıları (TASK 022). POST_NOTIFICATIONS: Android
         13+ runtime izni (bağlamsal istenir). SCHEDULE_EXACT_ALARM: vakit
         hassasiyeti için kesin alarm (runtime'da canScheduleExactNotifications
         kontrol edilir, yoksa inexact'e düşülür). USE_EXACT_ALARM KULLANILMAZ
         (yalnız alarm-saati uygulamaları içindir). Arka plan konumu/FCM YOK. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- flutter_local_notifications: yeniden başlatma / uygulama güncellemesi
         sonrası zamanlanmış namaz hatırlatıcılarının yeniden kurulması için
         gereklidir (ScheduledNotificationBootReceiver). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Ağ erişimi (TASK 040/041): Firebase callable meali + MP3Quran ayet
         sesi. Tüm adresler HTTPS — cleartext trafik AÇILMAZ. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Arka plan Kur'an sesi (TASK 045, audio_service): media playback
         foreground service + ekran kapalıyken oynatma. BOOT_COMPLETED,
         mikrofon, READ_MEDIA_AUDIO veya pil optimizasyonu bypass'ı YOK. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <application
        android:label="Bismillah"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Kur'an arka plan ses servisi (TASK 045, audio_service dokümanı):
             media session bildirimi + kilit ekranı kontrolleri. exported
             değerleri audio_service kurulum dokümanıyla birebir aynıdır. -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
        <!-- Required by flutter_local_notifications for scheduled reminders and
             rescheduling after reboot/app update. Both receivers are NOT exported. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- TASK 056A: resmî Diyanet kaynak sayfalarını SİSTEM TARAYICISINDA
             açabilmek için gereken minimum paket görünürlüğü (Android 11+).
             Yalnız https şeması bildirilir; WebView eklenmez. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
