# Task Index

Status values: COMPLETED · IN PROGRESS · PLANNED · BLOCKED · DEFERRED · SUPERSEDED.
The live Git history is authoritative; this index is a human-readable summary.

## Historical (TASK 001–060)

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 001–060 | COMPLETED (grouped) | Foundation | Flutter foundation, 5-tab nav, TR/EN/AR+RTL, onboarding, Prayer, Quran, Learn gate, Profile, local Assistant | Git history (grouped; exact per-task titles not reconstructable) | — |

## Public alpha and stabilization (061–068A)

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 061A-2 | COMPLETED | Public alpha | License, attribution, content policy | main history | — |
| TASK 061A-3 | COMPLETED | Public alpha | Public docs and CI | main history | — |
| TASK 061B | COMPLETED | Public alpha | Public alpha merge + `v0.1.0-alpha.1` | tag `v0.1.0-alpha.1` | — |
| TASK 062A | COMPLETED | Stabilization | Dependency triage (PRs #1–#6) | audit report | — |
| TASK 062B | COMPLETED | Stabilization | Master project audit | MASTER_PROJECT_REPORT | — |
| TASK 063 | COMPLETED | CP09 | package_info_plus 10.2.1 | PR #5 | — |
| TASK 064 | COMPLETED | CP09 | Drift schema + persistence baseline (11 tests) | PR #7 | — |
| TASK 065 | COMPLETED | CP09 | Merge Drift baseline | PR #7 merge | — |
| TASK 066 | BLOCKED | CP09 | Drift official CLI toolchain incompatibility | analyzer 13 vs Flutter 3.44.6 | revisit after SDK upgrade |
| TASK 067 | COMPLETED | CP09 | flutter-action v2.23.0 (+ closed PR #2) | PR #8 | — |
| TASK 068 | COMPLETED | CP09 | Node.js 22 Functions runtime and Functions CI | PR #9, merge `c847c4b` | — |
| TASK 068A | COMPLETED | CP09 | Permanent Claude project memory | PR #10, merge `c167454` | — |
| TASK 069 | COMPLETED | CP09 | fast-xml-parser 5.10.1 validation on Node 22 (23/23) | supersedes PR #6 | — |
| TASK 070 | SUPERSEDED | CP09 | 22.1.0 update — blocked by pre-existing manifest gap; work moved to 070A | local `a05f439` (unpushed) | reapply in TASK 070C |
| TASK 070A | COMPLETED | CP09 | Declare scheduled-notification receivers + manifest regression tests (589 tests) | PR #13 | — |
| TASK 070B | COMPLETED | CP09 | Samsung A36 / Android 16 device validation of manifest fix; reboot delivery deferred | PR #13, device report | — (closed by TASK 071) |
| TASK 070C | COMPLETED | CP09 | flutter_local_notifications 22.1.0 reapplied on validated manifest fix | PR #14, `93d1c86` | — |
| TASK 070D | COMPLETED | CP09 | Exact-alarm permission deep-link UX + 6 tests (focused 26, full 595) | PR #14, `50dacef` | — |
| TASK 071 | COMPLETED | CP09 | Final Samsung A36 end-to-end validation incl. reboot physical delivery; PR #14 merged | PR #14, device report | — |
| TASK 072 | COMPLETED | CP09 | Sync-queue audit: atomic producer + tested queue, NO consumer/remote; verdict READY FOR LOCAL QUEUE HARDENING ONLY (P0 0 / P1 6 / P2 4) | `TASK_072_SYNC_QUEUE_AUDIT.md` | — |
| TASK 073 | COMPLETED | CP09 | Local sync-queue hardening: backoff policy + failure taxonomy + recordFailure + pruning + stale recovery + diagnostics (sync 70/70, full 629); verdict READY FOR CONTROLLED REMOTE SYNC IMPLEMENTATION; remote sync stays disabled | `TASK_073_LOCAL_SYNC_QUEUE_HARDENING.md` | — |
| TASK 074 | COMPLETED | CP09 | Firebase security readiness audit: Rules/App Check/emulator/staging ABSENT (P1 blockers); callable auth-gated + secret-managed but deployed on EOL nodejs20; secret scan clean; verdict READY FOR LOCAL SECURITY HARDENING ONLY (P0 0 / P1 6 / P2 5) | `TASK_074_FIREBASE_SECURITY_READINESS_AUDIT.md` | — |
| TASK 075 | COMPLETED | CP09 | CP09 full regression checkpoint: all baselines re-run unchanged (analyze clean, 629/629, canonical sync 70/70 — `test/features/sync` alone 52, storage 11/11, Functions 23/23, debug APK SUCCESS); no dependency/lockfile change; authoritative Firebase gate order G1–G14; deployed nodejs20 drift re-verified (P1); npm audit 13 (0 critical / 5 high dev-only `eslint` chain / 8 moderate production `firebase-admin` chain); verdict **CP09 COMPLETE — TECHNICALLY STABLE**; product gate **READY TO ENTER NEXT LOCAL-FIRST PRODUCT CHECKPOINT**; P0 0 / P1 15 / P2 12 / deferred 5 | `TASK_075_CP09_TECHNICAL_STABILIZATION_CHECKPOINT.md` | run TASK 076 |

## CP10 — Today and 30-day plan

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 076 | COMPLETED | CP10 | DailyPlan local persistence: first implementation of the existing `DailyPlanRepository` via a **temporary versioned key-value envelope** (persistence version 1, key `bismillah.daily_plans`); canonical **per-day** model preserved (30-day frame = 30 records) and the brief's conflicting 30-day-snapshot model rejected with owner approval; typed corruption failures, no silent overwrite; 67 focused tests, full suite 629 → **696**; **no Drift schema/dependency/generation/UI/Firebase/remote-sync change** | `TASK_076_DAILY_PLAN_LOCAL_PERSISTENCE.md` | run TASK 077 |
| TASK 077 | COMPLETED | CP10 | Daily plan state machine: `DailyPlanController` + sealed `DailyPlanState` (Loading/Empty/Available/Corrupt/Failure); epoch counter blocks stale results; latest-valid-completion-wins save rule; single watch subscription cancelled on day switch/disposal; bootstrap never loads it. Includes approved contract fix **`StorageCorruptionFailure`** (type-only failure mapping; no signature/l10n/envelope/key change). 43 focused tests; persistence 67 → **70**; full 696 → **742**; **no generation/UI/Firebase/Drift/remote-sync change** | `TASK_077_DAILY_PLAN_STATE_MACHINE.md` | run TASK 078 |
| TASK 078 | COMPLETED | CP10 | Onboarding profile mapping: `DailyPlanProfileType` (**8** canonical §10 buckets with stable IDs) + pure `OnboardingProfileMapper` over the really implemented three-axis `OnboardingPreferences`; the 16-question `OnboardingAnswers` model is **unused scaffolding** and doc §474's recipe was unusable (its inputs are not collected) — stop gate fired, Option A approved. **Approved product decision:** priority justBeginning→beginner, rebuildingRoutine→returning, strengthening+focused→advanced, light→low_time, else goals prayer→quran→dhikr→learning. Sealed result Mapped/Incomplete only. 78 focused tests; full 742 → **820**; **no generation/UI/persistence/Drift/Firebase/remote-sync change** | `TASK_078_ONBOARDING_PROFILE_MAPPING.md` | run TASK 079 |
| TASK 079 | COMPLETED | CP10 | Deterministic daily plan generator (**skeleton + item-source contract**): `DailyPlanGenerator` emits exactly **30 per-day records**; `DailyPlanGenerationRequest` validates profile×pace against TASK 078 rules. Stop gate fired — dhikr/dua/reflection have **no content** (interfaces only), so Option B approved: content comes from TASK 080/081/082 sources. **Approved decisions:** pace 5/10/20 + `advanced`+`focused`→30; zero-based four-phase `weekIndex` (21–29→3, never 4, 7/7/7/9). Default empty source ⇒ 30 valid plans with empty `items`. Stable item ID `rule-engine-v1:<dayKey>:<templateId>:<slot>`. 63 focused tests; full 820 → **883**; **no persistence write/UI/Drift/Firebase/remote-sync change; no religious content invented** | `TASK_079_DETERMINISTIC_DAILY_PLAN_GENERATOR.md` | run TASK 080 |
| TASK 080 | COMPLETED | CP10 | Prayer plan items — **first approved `DailyPlanItemSource`**: `PrayerDailyPlanItemSource` emits `prayer_track_daily` / `prayer_on_time_daily` from goals `trackPrayers` / `prayOnTime`, in that fixed order, 1 estimated **interaction** minute each. Items come from **goals, not the profile**; identical across all 8 profiles and 4 phases. Representation gate passed with **no domain change** (`templateId` already flows into the item ID); `targetRef`/`sizeParam` null — no prayer name or count claimed. **No prayer-time calc, location, notification, `PrayerLog` read, persistence, UI, Firebase or remote sync; no composition added (arrives with TASK 081).** 62 focused tests; full 883 → **945** | `TASK_080_PRAYER_PLAN_ITEMS.md` | run TASK 081 |
| TASK 081 | COMPLETED | CP10 | Quran plan items **+ ordered source composition**: `QuranDailyPlanItemSource` emits one neutral `quran_continue_daily` (2 interaction min) when `quranHabit` is present — **no surah/ayah/juz/page/translation/reciter/audio/quantity assigned**, `targetRef`/`sizeParam` null, no Quran repository/progress/audio read. `CompositeDailyPlanItemSource` runs an explicitly ordered child list once per child per day in **Prayer → Quran → Learn** order; first typed failure propagates with **no partial output**; **cross-source** duplicate template IDs rejected (`ValidationFailure`, no new failure type or l10n key); empty child list ⇒ empty success. Generator unchanged (composite implements the same single-source interface); appending Learn later cannot shift Prayer/Quran slots or final IDs. Identical across 8 profiles and 4 phases. 70 focused tests; full 945 → **1015**; Quran feature suite **89/89** | `TASK_081_QURAN_PLAN_ITEMS.md` | run TASK 082 |
| TASK 082 | COMPLETED | CP10 | Source-verified Learn plan items — **third and final core `DailyPlanItemSource`**: `LearnDailyPlanItemSource` emits one `learn_article_<articleId>` item per day (1 interaction min) when `islamicKnowledge` is present; `dayOffset` **is** the catalog index (no modulo, repetition, randomness, locale/profile/phase reordering). `targetRef` = stable **published + source-verified** article ID; `sizeParam` null; **no title/summary/body/translation/source text copied**; **no Learn repository or asset bundle read during generation**. Immutable versioned `LearnDailyPlanCatalog.v1` (30 entries): 0–10 follow the existing editorial `beginnerPathOrder` (published only), 11–29 are an approved TASK 082 product decision grouped by `categories.json` `sortOrder`. Content gate passed with **no content change** — exactly 30 eligible (`published` + `sourceBodyReview` + publication gate), identical stable ID sets in TR/EN/AR, 2 `scholarlyReviewPending` excluded. `CoreDailyPlanItemSource` bundles **Prayer → Quran → Learn** (`const`, no provider, no bootstrap wiring); composite and generator **unchanged**; Prayer/Quran slots and final IDs unchanged on all 30 days. Complete core day = **5 min**, accepted exactly by `light`. Typed `LearnPlanCatalogIssue`; **no new `AppFailure` subtype or l10n key**. 163 focused tests; full 1015 → **1178**; Learn feature **108/108** (measured) | `TASK_082_LEARN_PLAN_ITEMS.md` | run TASK 083 |
| TASK 083 | COMPLETED | CP10 | Today task UI — **first consumer** of the TASK 077 state machine. `TodayPlanSection` renders all five sealed states: Loading (**non-animated** neutral skeleton — a spinner blocks every `pumpAndSettle` test that mounts Today), Empty (neutral text, **no fake "generate plan" button**), Available (selected day, `n/total` progress, `AppProgressBar`, ordered cards), Corrupt (calm "nothing was deleted", no retry, no auto-reset), Failure (neutral message + `retry()`). Pure `TodayPlanItemPresentation` maps template ID → neutral localized title; Learn titles resolve via the existing **published-only** `LearningKnowledgeRepository`, and unresolved/removed/failing articles fall back to a neutral label — **no fabricated title, no crash, no raw template/article ID or generator version on screen**. Order is the plan's own Prayer → Quran → Learn (proved not re-sorted by localized text). **Completion implemented**: `toggleItemCompletion` preserves all other plan fields, uses the injected `AppClock`, persists via existing `savePlan`, blocks duplicate writes, never generates or touches another day — **no new storage key, no envelope-version change**. Day navigation **deferred to TASK 084**; one watch subscription asserted. Free core — no paywall/ad/supporter/donation element (asserted). 53 focused tests; full 1178 → **1231** | `TASK_083_TODAY_TASK_UI.md` | run TASK 084 |
| TASK 083A | COMPLETED | CP10 | Initial DailyPlan orchestration — **controlled insertion** between TASK 083 and TASK 084 (nothing renumbered) closing the gap where the generator was never invoked. `InitialDailyPlanOrchestrator` runs preferences → `OnboardingProfileMapper` → request → `DailyPlanGenerator` + `CoreDailyPlanItemSource` → **one atomic write**; start day from the injected `AppClock` (no `DateTime.now()`). New `DailyPlanRepository.savePlans(List)` is a **single logical envelope write** (read-all → merge → encode once → write once): empty batch and duplicate `DayKey`s rejected, corrupt envelope never overwritten, unrelated days preserved, watch events only after success — **30 sequential `savePlan` calls forbidden**. Typed outcomes Created/AlreadyAvailable/OnboardingIncomplete/RangeConflict/GenerationFailed/PersistenceFailed carry no raw answers, JSON, keys, UID or exception text. **Idempotent**: complete range → no-op with completion state and timestamps untouched; **partial range → typed conflict, never auto-filled or overwritten**; concurrent calls memoized to one write. Onboarding completion succeeds **only after** the plan exists (failure keeps the gate closed and preferences persisted for safe retry). `InitialDailyPlanBootstrapController` covers already-onboarded users, running **once per lifecycle, never in `build`**, with explicit `retry()`. 73 focused tests (orchestrator 59, `savePlans` contract 11, Today section 3); full 1231 → **1304**; persistence suite 70 → **81**. **No storage-key or envelope-version change, no Drift, no Firebase, no remote sync, no premium work** | `TASK_083A_INITIAL_DAILY_PLAN_ORCHESTRATION.md` | run TASK 084 |
| TASK 084 | COMPLETED | CP10 | Missed-day recovery and gentle rollover. `TodayDayController` owns the displayed day: `start()` (bootstrap once + select), `onAppResumed()` via `WidgetsBindingObserver` (**no-op when the day is unchanged**) and **one armed one-shot** injected `DayRolloverScheduler` for local midnight — **no periodic polling**, cancelled on disposal. Boundary = `DateTime(y, m, d + 1)` − local now, so **24h is never assumed** (DST/month/year/leap safe); `DayKey` only from `DayKey.fromLocal(clock.nowLocal())`, no UTC shift, no `DateTime.now()` (source-asserted). Generation counter blocks an old day's late read. **Missed day** = past `DayKey` with a plan **and** items **and** zero completions; absent/empty/corrupt days, today and future days are **not** user-missed and each **breaks** the chain; 30-day lookback cap. Pure `MissedDayCalculator`; `MissedDayRecovery` is **not a streak model** (nothing persisted, no score/badge/rank, count never rendered). **Historical integrity**: nothing auto-completed, failed, copied forward, deleted, reordered or re-timed — recovery path does **zero writes** (asserted). `TodayRecoveryNote` sits above the plan, blocks no task, no modal/animation/error colour, and hides once any current-day task is marked — **no new persistence key**. At **3+** days only the copy softens; full plan, Prayer → Quran → Learn order and minute budget unchanged. Missing/out-of-range day keeps honest Empty — nothing generated, no copy-forward, no extension past day 30. Also fixed: `DailyPlanController.loadDay` still cancels the old watch subscription but **no longer awaits** it (`cancel()` does not resolve promptly for every stream and hung the second day load). 88 focused tests; full 1304 → **1392** | `TASK_084_MISSED_DAY_RECOVERY.md` | run TASK 085 |
| TASK 085 | COMPLETED | CP10 | 30-day plan and CP10 checkpoint. Verdict **CP10 COMPLETE — 30-DAY LOCAL PLAN FLOW STABLE**; product gate **READY TO ENTER CP11**. Full chain validated against executed suites (generator 63 · mapper 78 · Learn 163 · orchestrator 59 · persistence 81 · state machine 43 · rollover 82 · Today UI 62) plus a new end-to-end harness using the **real** `SharedPrefsDailyPlanRepository`, so "restart" means a new container **and** a new repository over the same stored bytes. **One checkpoint-blocking defect found and fixed:** TASK 084's `unawaited(subscription.cancel())` contained no errors, so a failing `cancel()` escaped as an **unhandled asynchronous error**; `_cancelQuietly` now uses `catchError`, proved load-bearing by reverting it. Awaited cancellation was **not** restored (it is what hung the day switch), and real watch failures still surface via `listen(onError:)`. Subscription audit all-pass: rapid A→B→C never sticks in Loading, A/B events cannot replace C, 12 switches leave **1** active listener, disposal drops it to 0, late cancels cannot mutate newer state. Day 31 stays honest Empty (no extension); historical plans byte-identical before/after recovery. 30 focused tests; full 1392 → **1422**; Functions **23/23** (Node v22.22.0 / npm 10.9.4). **No schema/key/envelope/Drift/Firebase/remote/premium change** | `TASK_085_CP10_30_DAY_PLAN_CHECKPOINT.md` | run TASK 086 |

## CP11 — Learn and Assistant depth

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 086 | COMPLETED | CP11 | Content-source matrix — first task of CP11. Canonical governance matrix at `docs/CONTENT_SOURCE_MATRIX.md`: **22 rows** with stable kebab-case IDs covering Quran text/metadata/page-map/translation/search/audio/reciter-catalog, the **inactive** remote Diyanet callable, Learn articles/prayer-education/categories/source-registry/plan-catalog, prayer-time calculation, prayer labels, dua, dhikr, onboarding copy, Today plan copy, Assistant retrieval corpus, Assistant safety copy and the Profile source registry. Status counts **READY 8 · READY WITH DOCUMENTED LIMITATION 11 · REVIEW REQUIRED 1 · BLOCKED 0 · NOT IMPLEMENTED 2**. Every figure recomputed from shipped assets: **30 published / 2 `scholarlyReviewPending`** per locale, TR/EN/AR carrying **identical stable ID sets AND identical review statuses**, all 30 published on `sourceBodyReview` with locator + evidence + `verifiedAt`, every referenced source ID resolving to `sources.json`. **No P0/P1**: no sourceless published content, no registry mismatch, no divergent-subject locale set, no pending content exposed as published, no Assistant path to unpublished material, no evidenced licensing conflict for shipping content. **Six P2 findings**: F1 Assistant persistence gate excludes `worshipRule` while `isSensitiveVerdict` includes it and has **no production caller** (TASK 094); F2 `app_source_reference.dart` duplicates `sources.json` uncross-checked (TASK 094); F3 QuranEnc licensing **UNRESOLVED**; F4 deployed Diyanet callable client-inactive, licensing unresolved, EOL `nodejs20`, App Check TODO; F5 3/6 sources unused + 11/20 categories empty (TASK 087–090, TASK 092); F6 Tanzil/QuranEnc never fully diffed upstream. Assistant boundary fixed to published Learn + category titles + source registry; **`internal-ui-copy` is never religious authority**. **No religious content written, edited, repaired or reclassified; zero production Dart/asset files changed.** 14 focused tests; full suite 1422 → **1436**; analyze clean; Functions not re-run (untouched) | `TASK_086_CONTENT_SOURCE_MATRIX.md` | run TASK 087 |
| TASK 087 | COMPLETED | CP11 | Learn pack: Hadith, Seerah, Prophets. Owner-fixed scope **3+3+3** — **9 new published articles / 27 localized records** populating `cat-hadith`, `cat-seerah`, `cat-prophets`. Per locale **41 records, 39 published** (was 32/30); categories **9 → 12 of 20**; sources grounding published content **1 → 3 of 7**. New source `diyanet-hz-muhammedin-hayati` (*Hz. Muhammed'in Hayatı*, Casim Avcı – Mevlâna İdris, **Çocuk Kitapları 302**, 16. Baskı Mart 2025, ISBN 978-975-19-6627-8) with series/edition/authorship explicit and **no Diyanet endorsement implied**; `diyanet-hadislerle-islam` grounds published content for the first time; `diyanet-islam-ilmihali` confirmed **34. Baskı 2019** from the PDF colophon. **Content gate blocked twice, both correct**: the proposed Seerah/Prophets URLs are dead (`dijital.diyanet.gov.tr` → HTTP/0.9; Prophets PDF → `hata.html`) and were replaced with working official sources; `art-guzel-ahlak-ve-yardimlasma` was rejected because *Hadislerle İslâm* C.1 s.250 covers the first revelation, not good character — replaced only on explicit owner permission. **Direct-source rule honoured** (PDFs downloaded outside the repo, text extracted directly, nothing committed, no summary used as evidence). Review provenance reported **AMBIGUOUS**; no verified status written until the owner recorded an explicit source-fidelity review; proposed `CONTENT_POLICY.md` wording prepared but **not applied**. **TASK 082 catalog untouched** (30 entries, order, version); none of the 9 IDs enters it. **Four merged TASK 082 "library == catalog" assertions reconciled in-branch on owner decision** — subset invariant **strengthened** to all locales; verification/publication/locale/review gates **not** weakened. Excluded by design: hadith grading, personal rulings, sectarian argument, reward guarantees, miracle framing, the 28-prophet count and disputed classifications, the exact Veda Haccı attendance figure in prose. No Assistant/Quran/Prayer/persistence/Firebase/remote/premium change. 34 focused tests; catalog suite 40/40; full suite 1436 → **1472**; analyze clean; Functions untouched | `TASK_087_LEARN_HADITH_SEERAH_PROPHETS.md` | run TASK 088 |
| TASK 088 | COMPLETED | CP11 | Learn pack: Dua, Family, Halal foundations. Roadmap defines no count and TASK 087's 3+3+3 was **not** inherited; owner fixed **4 articles** after a PARTIALLY READY Phase A gate — `cat-dua` 1, `cat-family` 2, `cat-halal` 1 (12 localized records). Per locale **45 records / 43 published** (was 41/39); categories **12 → 15 of 20**; **no new source record** (registry stays at 7, asserted). Gate findings: the İlmihal has no standalone dua chapter and its general dua page (s.163) **extracts with corrupted glyphs**; the Hadislerle İslâm dua chapter's spine is *guaranteed acceptance and special times* (s.35–37, 40–41) which is excluded, so `art-dua-nedir` rests on **s.40 alone** — disclosed, not padded; and `cat-dua` was already occupied by the pending `art-dua-adabi` (**TASK 091**, untouched). Second family article renamed `art-anne-babaya-saygi-ve-nezaket` and narrowed to courtesy — the source's blanket-obedience and maintenance items dropped. Halal article limited to definitions, attributes them explicitly to the İlmihal, and redirects personal cases to the competent authority; `art-haramin-cesitleri` stays out of scope (SCHOLARLY REVIEW REQUIRED). "Attentive listening" was **omitted** because it is not in the source body. **One TASK 087 test corrected**: its absolute totals (`32+9=41`, `30+9=39`) were replaced by growth-tolerant assertions plus a check that all nine TASK 087 articles remain published and verified. TASK 082 catalog untouched; none of the 4 IDs enters it. No Assistant/Quran/Prayer/persistence/Firebase/remote/premium change. 29 focused tests; full suite 1472 → **1501**; analyze clean; Functions untouched | `TASK_088_LEARN_DUA_FAMILY_HALAL.md` | run TASK 089 |
| TASK 089 | COMPLETED | CP11 | Learn pack: Women, Afterlife, Islamic history. Owner scope **5 articles** — `cat-afterlife` 3, `cat-women` 1, `cat-history` 1 (15 localized records). Per locale **50 records / 48 published** (was 45/43); categories **15 → 18 of 20** (only `cat-madhhabs` and `cat-calendar` remain empty); **no new source record** (registry stays at 7). **The pointed history section was rejected, not forced:** Hadislerle İslâm C.1 s. 66–82 has no *rihle* chapter — s. 69 is military expansion, s. 70 gives openly conflicting sahâbî counts (12.304 / ~4.000 / 1.500), s. 71 is fitne, sects and Umayyad-era fabrication, and s. 72–82 is authenticity grading. `art-ilim-yolculuklari` was therefore dropped and replaced, on owner approval, by `art-kuranin-yazilmasi-ve-cogaltilmasi` from İslam İlmihali s. 55–56 (written revelation → Abu Bakr's single mushaf after Yemâme → Uthman's seven copies). **One locator was corrected** during drafting: `art-ahiret-nedir` is recorded as **s. 65–67**, not s. 65, because the "Ahiret Günü" definition it summarises sits on s. 67. Afterlife articles exclude cennet/cehennem description, kabir azabı, şefaat, mizan/sırat, salvation judgments and fear framing; the Women article excludes the governance hadith (C.4 s. 236), mahrem-travel rulings (s. 238), covering rulings, the war-participation clause and s. 240's normative social conclusion; the History article excludes conquest framing, caliphal legitimacy, sectarian reading and manuscript criticism, and preserves the source's own "yaklaşık" and "yedi kadar" hedging. `art-olum-nedir` was **not** added (asserted). **Two stale empty-category fixtures were fixed durably**: `learn_repository_test` and `learn_screens_test` hard-coded `cat-history` as their empty exemplar; both now derive an empty category from the shipped assets, so future packs cannot break them. TASK 082 catalog untouched; none of the 5 IDs enters it. No Assistant/Quran/Prayer/persistence/Firebase/remote/premium change. 27 focused tests; Learn + catalog **238/238**; full suite 1501 → **1528**; analyze clean; Functions untouched | `TASK_089_LEARN_WOMEN_AFTERLIFE_HISTORY.md` | run TASK 090 |
| TASK 090 | COMPLETED | CP11 | Learn pack: Madhhabs, Islamic calendar and remaining gaps — **the final Learn pack**. Owner scope **6 articles** — `cat-madhhabs` 3, `cat-calendar` 3 (18 localized records). Per locale **56 records / 54 published** (was 50/48); populated categories **18 → 20 of 20** — **no category is empty any more**; registry **7 → 8**; sources grounding published content **3 → 4 of 8**. "Remaining gaps" was read from the repository, which defines it **only** as the two empty categories and defines **no count or acceptance criteria**; the thin categories were left alone. **Four evidenced candidates were classified SCHOLARLY REVIEW REQUIRED and NOT published** — the contested Muâz b. Cebel narrative, the s. 30-31 **Ehl-i Sünnet / Ehl-i Bidat** framing, the rü'yet-i hilâl-bound calendar/authority distinction, and Tevbe 9/36-37 with its war/nesî' and haram-month context — absence asserted by test. **A new source was genuinely required**: the İlmihal has "takvim" on two pages only and carries **no month list, epoch or year length**, so `diyanet-vakit-hesaplama` was registered — the registry's first maintained **web page**, typed `officialPublication` (**no enum change**), passing the existing `diyanet.gov.tr` allowlist unchanged, with **author, edition, ISBN and publication date absent, not invented** (asserted) and volatility recorded as a limitation. **Owner binding correction applied**: the source's exact Gregorian conversion dates are excluded from **all** shipped content including `evidenceSummary`, and not replaced by another exact date (asserted across three locales). Locator disclosed as **s. 33-38** (not s. 33 + 36-38) because the Sunnah definition sits on s. 34. Scholarly difference on sükûtî icma **stated, not hidden**, via `differenceOfOpinion`. **Three stale fixtures repaired durably**: `task_088` and `task_089` froze `sources.length == 7` and now assert intent while allowing registry growth; `learn_screens_test`'s "Hazırlanıyor" assertion moved onto the synthetic `EmptyCategoryFixtureBundle`. TASK 082 catalog untouched (30); TASK 091's 2 pending records untouched. No Assistant/Quran/Prayer/persistence/Firebase/remote/premium change; no schema, enum, key or envelope change. 37 focused tests; Learn **236/236**; Learn + catalog **276/276**; governance **327/327**; full suite **1529 → 1566** (stored 1528 corrected to a measured 1529); analyze clean; Functions untouched | `TASK_090_LEARN_MADHHABS_CALENDAR.md` | run TASK 091 |
| TASK 091 | COMPLETED | CP11 | Review pending Learn articles. Scope read from the roadmap, which defines TASK 091 in **one line with no acceptance criteria** — so it covers **only** the two long-standing pending records; the four TASK 090 candidates stay deferred. Per locale **56 records / 55 published / 1 pending** (was 56/54/2); categories stay **20 of 20**, registry **8**, catalog **30**. **`art-kuran-okumaya-baslangic` rewritten and published**: its old body was unsupported by its own registered source (`diyanet-kuran-portali` is a Quran text/translation service, and the record had an **empty locator and empty evidence summary**), so it was re-grounded on **İslam İlmihali (34. Baskı, 2019), Kur'an-ı Kerim'e Karşı Görevlerimiz — b) Kur'an-ı Kerim'i Öğrenmek, s. 58**, read directly. It now states only what s. 58 states: learning the Quran = learning to read it **and** working to understand its meaning, with tajwid part of that learning. **Removed**: the five-step list (on no source page), the s. 58 **virtue hadith**, the s. 58 **"farzdır"** ruling, the reward framing, and all pace/mastery/programme/daily-amount claims. Title → "Kur'an'ı öğrenmek ne demektir?"; `contentType` → `ilmihalKnowledge`; `beginnerPathOrder` **11 preserved**; slugs unchanged; **no new source record**. **`art-dua-adabi` stays `scholarlyReviewPending`** — title/summary/body/source/locator/translations **untouched**, only `isFeatured` → `false`; recorded that **qualified scholarly review is required**, the body is **not approved**, the **Arabic strengthens one sensitive claim** vs the Turkish canon, and narrowing would **duplicate `art-dua-nedir`**. `diyanet-kuran-portali` now grounds no Learn article and is **kept registered on purpose**. **Six existing assertions re-pointed, not weakened** — three pack tests, the Assistant pending exemplar, the repository visibility test (now also asserting the published article **is** reachable and verified), and two catalog assertions that had frozen "catalog head == every published beginner-path member" and "orders 11/12 pending"; both now encode the durable invariant. One stale catalog doc comment corrected; catalog value unchanged. 24 focused tests; Learn+Assistant+catalog+content **379/379**; full suite **1566 → 1590**; analyze clean; Functions untouched | `TASK_091_REVIEW_PENDING_LEARN_ARTICLES.md` | run TASK 092 |
| TASK 092 | COMPLETED | CP11 | Official-answer / fatwa-source index foundation. Roadmap, `TASK_INDEX`, `CURRENT_BASELINE` and the state JSON all define TASK 092 in **one line with no acceptance criteria**, no storage format, no record count and no approval gate — scope was read narrowly, as TASK 090/091 did for their own underspecified entries. Delivered: **the index contract only, with ZERO production records**, no consumer, no UI, no network, no persistence key, no migration, no dependency. New module `lib/features/official_answers/` — record entity (**no answer body field at all**, so it cannot hold a composed ruling), per-locale index rejecting duplicate ids, read-only repository contract (no write/compose API), strict pure-Dart parser throwing the existing `ContentSchemaError`, and an offline `AssetBundle` loader. Assets `index_{tr,en,ar}.json` each exactly `{"schemaVersion":1,"locale":"..","answers":[]}`. **Owner decision — a DEDICATED publication gate, deliberately stricter than the Learn gate:** `SourceVerification.satisfiesPublicationGate` never inspects `verifiedBy`, so reusing it would let `editorialReview` publish a fatwa-shaped record; it is now only **one component**. A published retrievable record requires `published` + source body verified + **`verifiedBy == scholarlyReview`** (`editorialReview` and `automatedSourceCheck` **fail**) + an **approved authority source** (only the two authority-typed ids `diyanet-din-isleri-yuksek-kurulu`/`fatwa` and `diyanet-dini-soru-hizmetleri`/`officialAnswer`, **read from `sources.json`, not invented**, plus `isOfficial` and `sourceType` check) + a non-empty `https` `sourceUrl` on the existing allowlist and **strictly deeper** than the source `canonicalUrl` (a bare fatwa landing page cannot pose as an exact answer address) + a non-empty exact locator + stable unique `oa-` id with no `:` + locale consistency + `isGeneralInformationOnly`. Issues are a typed `OfficialAnswerGateIssue` list, never free text. Enforced at **both** construction and the retrieval chokepoint; the `final class` record has one generative constructor, no `copyWith`/`fromJson` and an unmodifiable list, so a published-but-ungated record **cannot be constructed** — the retrieval filter is retained against a future shortcut. **The Learn gate was NOT touched** (no file under `lib/features/learn/` or `assets/content/learn/` modified) and a **negative-control test asserts `editorialReview` still satisfies the LEARN gate**, so a later refactor cannot globally tighten Learn silently. `verifiedBy: scholarlyReview` is a **data field the gate checks, NOT evidence a qualified human review occurred** — no agent may record it. Privacy: no raw user question stored/logged/echoed (`topic`/`summary` are authored asset fields), no network/Firebase/SharedPreferences/new key, `getById` returns `null` and **never falls back to Learn**; `_guard` now contains `Error` as well as `Exception` because the record's own `ArgumentError` would otherwise escape and leak validation prose — failures collapse to a bare `StorageFailure`. **Pre-implementation Opus review returned BLOCKED with 7 blockers** (Learn gate admitted `editorialReview`; no `sourceUrl` field; any registered source incl. a book could ground a record; retrieval filtered on `isPublished` only; `_guard` missed `Error`; and a merged test asserted the forbidden `editorialReview` success as correct) — all were the owner's already-recorded requirements not yet implemented, so they were implemented, not escalated; **final Opus review PASS WITH FOLLOW-UPS**, all 7 verified closed. `docs/CONTENT_SOURCE_MATRIX.md` gains one row `official-answer-index`, **READY WITH DOCUMENTED LIMITATION** (contract shipped but grounds no content, so `READY` and `NOT IMPLEMENTED` would both be false), recording zero records, no consumer, qualified human review required, and that `isGeneralInformationOnly` is **declarative** — nothing verifies a body is not a personal ruling. `art-dua-adabi`, Learn content and `LearnDailyPlanCatalog.v1` (30) unchanged. 49 focused tests; content **14/14**; Learn+Assistant+content+catalog **379/379** (identical to TASK 091, no regression); full suite **1590 → 1639**; analyze clean; Functions untouched | `TASK_092_OFFICIAL_ANSWER_INDEX_FOUNDATION.md` | run TASK 093 |
| TASK 093 | DEFERRED | CP11 | Assistant retrieval ranking and no-source UX. **Owner decision: DEFERRED until real official-answer records exist.** Priority gate found ranking **already implemented** (7-tier weighted scorer: title 1000 → alias 900 → phrase 500 → keyword 60 → category 30 → summary 12 → section 4, deterministic tie-break, `maxArticles` 3, empty-result branch) and the **no-source UX already shipped** (`AssistantAnswerType.noVerifiedSource` / `officialFatwaRequired`, copy present in TR/EN/AR), with the Assistant already wired end-to-end (FAB → router → screen → controller → repository → history) and personal-verdict refusal already implemented. Its only new increment is wiring the TASK 092 official-answer consumer against an index holding **zero records** — user-invisible, and blocked in substance on the unowned qualified-human-reviewer decision. **Not recorded anywhere as a closed-alpha prerequisite** (closed alpha is TASK 101, behind CP12 TASK 095–100). Must consume `getPublished` only, never `getIndex` | roadmap CP11; TASK 094 report | revisit when real records exist |
| TASK 094 | COMPLETED | CP11 | Learn/Assistant security, language and RTL checkpoint — **COMPLETE.** The privacy/governance portion merged first (`e8b0de7`); the completion branch closed the rest. **Added: persistence-boundary defense-in-depth** — `SharedPrefsAssistantHistoryRepository.save` now runs the same `_pruneSensitive` pass as `load()`, so a DIRECT repository call cannot write sensitive history; pruning runs BEFORE the 20-message cap; `save` returns `ResultFuture<void>` so a failed write is not indistinguishable from success. **F2 CLOSED:** the four registered Diyanet entries in `app_source_reference.dart` now carry ONLY a `registrySourceId` and resolve name/language/URL from `sources.json` at runtime — nothing left to drift; Tanzil/QuranEnc/MP3Quran are infrastructure sources absent from the registry (asserted), so they are a single definition, not a copy; a missing id fails safely and visibly (no invented metadata, no silent skip). **Language:** `_t()` falls back to English, so a TR/EN/AR key-set equality test is the real guard; safety surfaces asserted present, distinct and Arabic-scripted; one new key `sourcesUnavailable` in all three locales; no copy rewritten. **RTL:** Assistant and content-sources screens assert `rtl`/`ltr` resolution, user bubble mirrors to the end side in both directions (proving logical not hard-coded alignment), refusal/no-source states render under RTL, Latin source name stays LTR inside the Arabic page, and RTL at 1.5x on a narrow screen does not overflow. No religious claim, article body, locator or source record changed; no dependency or Functions change. Boundary **15/15**; drift **8/8**; Assistant RTL **8/8**; profile+settings **65/65**; full suite **1658 -> 1705**; analyze clean. Original PARTIAL note: Closes **only** finding **F1** (Assistant sensitive-query persistence) and the formal **`editorialReview`** definition. **Still OPEN under TASK 094: language verification, RTL verification, and finding F2** (`app_source_reference.dart` duplicates `sources.json` with no cross-check) — no owning follow-up task; no TASK 094A invented. **Confirmed defect:** `assistant_providers.dart` decided persistence with an inline duplicate list omitting **`worshipRule`**, so sensitive worship-rule pairs — including raw user question text — **were persisted** to `bismillah.assistant_history`, against the `DO_NOT_BREAK.md` rule; the classifier's own `isSensitiveVerdict` already included `worshipRule` but had **zero production callers**. **Fix:** `isSensitiveVerdict` is now an **exhaustive `switch` with no `default`/wildcard**, so a new `AssistantQueryClass` is a **compile error** rather than a silent `false`; the provider calls it; **no duplicate sensitivity list remains in any persistence path** (grep-verified). Only that one key ever receives Assistant query text — no Drift table, no sync queue, no other key. **Legacy records (owner decision — delete, accepting bounded false positives):** `queryClass` was never persisted and `answerType` is not a usable proxy, but raw `text` is stored and `classify` is pure, so `load()` re-classifies stored text and drops a sensitive user message plus the immediately-following entry **only if `role == assistant`** — a **role check, never index parity**, because `save` caps via `sublist(length - 20)` and the list may begin with an orphan assistant message. The pruned list is **written back (real deletion)**, gated on pruning having removed something, because `_decodeMessage` drops corrupt records and an unconditional resave would have **permanently erased** them (asserted byte-for-byte). Cleanup is bounded (≤20), **idempotent**, single-key, preserves all unrelated keys, writes **no backup or quarantine copy**, contains `Error` as well as `Exception`, and **never logs the removed raw text**. **Accepted, documented limitation:** detection is keyword-based (`worshipRule` keys on `{'bozar','invalidate','يبطل'}`), so a benign line like "bu gürültü konsantrasyonumu bozar mı" **is deleted too** — proved by a test named honestly as an accepted false positive; the cleanup is **not** described as precise. **`clear()` silent failure fixed** (owner approved in scope): it returned success while the key could still hold raw text; `clear` is now `ResultFuture<void>`, the controller returns `bool`, failure leaves state untouched and suppresses the confirmation, both call sites updated, no new l10n copy invented. **Logging:** no Assistant code calls print/debugPrint/AppLogger/analytics; `NoOpAnalyticsService` sends nothing behind a `PrivacyGuard`; no failure path embeds query text; **no broader protection claimed than tests prove**. **`editorialReview` governance:** root `CONTENT_POLICY.md` and `docs/LEARN_CONTENT_REVIEW.md` now carry the same cross-referenced definition — owner/editor **source-fidelity and presentation review only**, explicitly **not** scholarly/fatwa/hadith-grading/legal review or Diyanet endorsement, with `scholarlyReview` preserved as the stronger gate. Scoped as **the requirement going forward**, stating the historical provenance of already-published records is **not re-verified** (TASK 087 recorded it **AMBIGUOUS**), so it cannot read as retroactive certification of the **55** shipped `editorialReview` records (vs **1** `scholarlyReview`). **No content reclassified, no publication status changed**; "approved"/"cleared" avoided. **Stale governance corrected:** `CONTENT_SOURCE_MATRIX.md` had F1 open including the now-false "no production caller"; F1 marked **CLOSED (TASK 094)** with original evidence preserved and the true new limitation recorded; `PRIVACY_MODEL.md` documents the retroactive cleanup and its accepted false positive. Learn article bodies, `art-dua-adabi`, `LearnDailyPlanCatalog.v1`, `official_answers/**` and `app_source_reference.dart` untouched; no dependency, schema, migration, new storage key or remote-sync change. Assistant **84/84**; focused TASK 094 file **9**; TASK 092 gate + content **63/63** (Learn `editorialReview` negative control still passes — no regression); content **14/14**; full suite **1639 → 1658**; analyze clean; Functions untouched. Opus pre-review **PASS WITH FOLLOW-UPS** (3 blockers + required owner acknowledgement of the accepted deletion); Opus final review **BLOCKED** on missing docs/matrix/tests, all closed | `TASK_094_ASSISTANT_PRIVACY_REVIEW_GOVERNANCE.md` | close language/RTL + F2 |

## CP12 — Prayer quality and beta

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 095 | IN PROGRESS — PR OPEN, NOT MERGED | CP12 | Qibla Direction MVP (Android). New `lib/features/qibla/` module. **Bearing math is pure and plugin-free**: `QiblaBearing.fromCoordinates` computes the initial great-circle bearing to `KaabaLocation` (**21.4225 N, 39.8262 E**, source and its limits documented in the constant file — not silently copied), normalizes every result to `[0, 360)`, and returns a **typed failure instead of a number** for invalid coordinates, the Kaaba point itself and its antipode, so `atan2(0, 0)` can never surface as a misleading "north". Verified against independently published Qibla directions (Istanbul ~151.6, London ~118.9, New York ~58.5, Jakarta ~295.2, Kuala Lumpur ~292.5, Cape Town ~23.4, Sydney ~277.5) at **1 degree** tolerance — a tighter tolerance would test precision the hardware cannot deliver. **No package was added**: the compass is an Android `EventChannel` over the platform's own `SensorManager` (`TYPE_ROTATION_VECTOR`, falling back to `TYPE_GEOMAGNETIC_ROTATION_VECTOR`), and the emitted heading is corrected to **true north** via `GeomagneticField` declination — if declination cannot be applied the reading is flagged `unreliable` rather than passed off as true north. Sensor and location both sit behind small injectable interfaces (`QiblaCompassService`, the existing `PrayerLocationService`); tests inject deterministic fake headings and **never assert physical compass accuracy**. **Location is reused, not re-requested**: the screen calls `currentLocationIfPermitted()` on open, so an already-granted permission raises **no new platform dialog** (asserted); the permission request fires only after the user reads why location is needed and taps. No background location, no persistence — a source scan asserts the module imports no SharedPreferences, Drift, Firebase, http or analytics. `PrayerLocationServiceDisabled` was added to the shared location result so "location service off" is an honest, separate state; the **Prayer times surface is unchanged** (it maps to the existing calm unavailable state). Fallbacks handled explicitly: service disabled, permission denied, permanently denied, location unavailable, calculation error, sensor unsupported, heading timeout (5 s), stream error and low-confidence reading — **when the sensor is missing the static bearing still renders** with a plain explanation. `HeadingSmoother` is a pure circular EMA with an emit threshold plus a 30-degree snap escape, so the needle neither shakes nor lags (asserted at controller level: noisy samples produce **zero** extra states, a real turn produces one immediately). TR/EN/AR strings complete and key-set equal, Arabic asserted to be Arabic script and distinct from Turkish; RTL resolution, 1.5x text on a 360px screen, semantics for bearing/alignment and reduced motion all covered. Free — no lock, badge or upgrade prompt. **Visual refinement (second pass, presentation only — bearing math, location, permission, sensor architecture, smoothing, routes and fallback logic all unchanged):** a code-drawn stylized Kaaba (no asset, no dependency) sits at the exact dial centre, **upright and never rotating**; its dark kiswah and gold hizam are an illustration palette documented as **not** the reserved premium `accentGold`, and a theme-sourced plinth keeps it legible on light and dark schemes. The dial gained layered surfaces, a soft depth shadow, 10°/30° tick hierarchy, localized cardinal letters (TR K/D/G/B · EN N/E/S/W · AR ش/ق/ج/غ) that are **counter-rotated to stay upright**, and a tapered Qibla needle with a filled arrowhead that reads clearly stronger than ordinary marks. Rotation is accumulated **unwrapped** so 359°→1° takes the short path, and animates over `AppMotion.tap`. Entering alignment plays **one** Kaaba scale emphasis (1→1.06→1), **one** fading halo ring and **one** light haptic — driven by an entry counter, so staying aligned never retriggers; leaving alignment simply returns. Under reduced motion no emphasis or halo plays at all and the state still changes immediately. Performance: each section watches only its own `select` projection, so a sensor sample rebuilds only the dial section and repaints only the rotating layer (plate, halo and Kaaba sit behind their own `RepaintBoundary`); every controller is disposed and nothing animates at rest. **70 focused tests** (`flutter test test/features/qibla`); full suite **1728 → 1798**; analyze clean; Functions untouched | branch `task/095-qibla-direction-mvp` | Samsung A36 visual validation, then merge |

| TASK 096 | COMPLETED | CP12 | Prayer calculation-method selection. **Source-truth gate first:** the engine is `adhan_dart 2.0.1` behind `AdhanPrayerTimeCalculator`; the previous default was `PrayerTimeCalculationMethod.turkiyeDiyanet` with `AsrCalculationMethod.standard` — and the calculator **hard-coded `CalculationMethodParameters.turkiye()` and silently IGNORED its own `method` argument**, so the parameter existed but did nothing. It now really applies, through **one exhaustive `switch`** (`adhanParametersFor`, no `default`/wildcard, so a new enum value is a compile error). **The default is unchanged** and users who never choose keep it: a missing, empty, unknown or wrong-typed stored value resolves to `null` → `defaultMethod`, and **`loadMethod` never writes back**, so the stored bytes survive untouched (asserted). **Institutional-name verification:** the `turkiye()` preset is adhan_dart's documented Diyanet-*approach* parameter set (fajr 18°, isha 17°, sunrise −7 / dhuhr +5 / asr +4 / maghrib +7), **not official Diyanet data**, so no user-visible string says "Diyanet" — the option is labelled by **region** ("Türkiye" / "تركيا") and the stale fixed header `prayerTimesMethodLabel` ("Türkiye hesaplama yöntemi") became the neutral "Hesaplama yöntemi" with the actually-used method name shown beside it, read from `times.method` (the result), not from a second setting. A test scans all three dictionaries for the word across every `prayerMethod*` key. **20 methods exposed**, each resolving to a real preset; `other` (fajr 0°/isha 0° — a blank custom template) and `jafari`/`tehran` (a different fiqh tradition, needing a separate approval decision) are excluded and the exclusion is **asserted, not silent**. Option explanations are **derived from the engine**: `AdhanPrayerCalculationMethodCatalog` reads `fajrAngle`/`ishaAngle`/`ishaInterval`/`methodAdjustments` from the same preset the calculation uses, so no angle or minute value is retyped in UI or localization and drift is structurally impossible. **Persistence:** new `PrayerCalculationMethodRepository` + `SharedPrefsPrayerCalculationMethodRepository` on the single key `bismillah.prayer_calculation_method` (stable enum `name`; the `bismillah.` prefix means the existing full local reset already clears it). **One authoritative source:** `prayerCalculationMethodProvider`, seeded before `runApp` by a bootstrap override exactly like the app locale. `PrayerTimesController` **watches** it, so Prayer and Today (which share that controller) recompute automatically — there is no separate cache to invalidate and a stale time cannot survive a successful change (asserted). No screen resolves the method from storage itself. **Notification policy: B, documented.** The method is persisted first, then reminders are rescheduled through the existing `PrayerReminderScheduler.reschedule`, which still cancels only Bismillah reminders before scheduling — so no duplicate and no past notification is created (asserted). Rescheduling **requests no new permission**: it reads `checkPermission()` and `currentLocationIfPermitted()`. The global reminders-enabled preference is never written; when reminders are **off**, nothing is scheduled and that is reported as success, not failure. Policy A was rejected because a failed write after successful scheduling would leave new-method notifications permanently contradicting old-method times with no deterministic repair. A failed reschedule returns the distinct `PrayerMethodRemindersNotUpdated` — **never reported as full success** — with a `retryReminderReschedule()` that reschedules only, without rewriting the method. `PrayerReminderScheduler` takes `method` as a **required** parameter of `reschedule` rather than a constructor field: making the provider watch the method provider created a circular dependency with the controller that changes it, and `required` still makes a forgotten call site a compile error. **Note:** the app has **no per-prayer** reminder toggles — reminders are one global boolean — so "preserve per-prayer preferences" is verified as preserving that preference and scheduling nothing while it is off. **UI:** a new `/prayer/calculation-method` push route inside the Prayer branch, reached from a single entry card in the existing Prayer card language (Qibla pattern); the screen reuses the language-settings option/note surfaces — no new component family. Confirmation is required and its text states that prayer times will be recalculated **and** enabled reminders rescheduled, plus that marked prayers and history stay unchanged; **cancelling calls nothing** (asserted: zero writes, state unchanged, no snackbar). Duplicate applies are blocked twice — the list is disabled while applying and the controller memoises an in-flight operation, so two concurrent calls produce **one** write. No dead rows, no endorsement claim (a permanent disclaimer explains that a method name identifies the publisher of the parameters, not an approval, and that times do not replace an official timetable). TR/EN/AR complete with per-method names, Arabic RTL, 1.5x text on a 360 px screen (including the Arabic confirm dialog), semantics per option, and reduced motion (durations collapse; nothing animates at rest). **Out of scope and untouched:** adhan audio, notification UI, Qibla, Assistant, Profile, Quran, remote sync, payments; **no dependency added**; **no madhhab/Asr selection and no manual minute offsets** (not in the canonical TASK 096 line); no schema, migration or envelope change; the 30-day plan is **not** regenerated (a `DailyPlan` stores no prayer times — a test asserts `bismillah.daily_plans` is byte-identical after a change). One merged assertion repaired, not weakened: the Prayer-screen test froze the old hard-coded "Türkiye hesaplama yöntemi" header and now asserts the neutral label **plus** the actually-used method name. **40 focused tests** (`flutter test test/features/prayer_times/task_096_calculation_method_test.dart test/features/prayer_times/task_096_calculation_method_screen_test.dart`); prayer + prayer_times + prayer_reminders **110/110**; full suite **1921 → 1961** (measured **1961/1961**, 0 failed, 0 skipped); analyze clean; Functions untouched. **Samsung A36 visual and functional validation PASSED** — the calculation-method screen works and is readable, changing the method really changes prayer times, Prayer and Today stay consistent, the selection persists after restart, prayer history and completed records are preserved, cancelling changes nothing, reminder enabled/disabled behaviour is preserved, no duplicate or misleading success behaviour was observed, and no RTL, large-text or visual blocker was found | PR #49 merged (CI **Analyze & Test pass, 3m49s**); main `4db8d2436abd3c22d1bca2aafbeba9fd1b295df7` | — |

## Owner-assigned UI tasks

| Task | Status | Checkpoint | Summary | Evidence | Next action |
|---|---|---|---|---|---|
| TASK 095D | IN PROGRESS — PR OPEN, NOT MERGED | Owner-assigned Assistant quality (not a roadmap task; **TASK 096 and every later roadmap number are unchanged**) | Assistant UX and alpha polish. **No external AI, no new dependency, no production official-answer record, no religious claim, source record or classification changed.** **Landing state:** the former empty state becomes a scoped explanation — what the Assistant does (searches Bismillah's **published, source-verified** content; shows the source and its exact page/section; links to Learn), then its **limits**, where the "not a source of fatwa" notice is the first and most visible line, plus an honest local-processing line ("questions are handled on this device; not sent to an external AI service"). No fake conversation, testimonial or AI-capability claim. **The Arabic example question was corrected, not the index:** a test runs all five suggestions per locale through the **real** asset-backed repository and requires a sourced answer, which caught `كيف أستعدّ للصلاة؟` returning `noVerifiedSource`; the wording moved to `كيف يكون الاستعداد للصلاة؟` (same meaning) so no suggestion promises an answer retrieval cannot return. **Refusal states are now distinct:** `officialFatwaRequired` gets its own badge instead of sharing the personal-guidance badge, and each refusal/no-source card ends with a safe next step (read the general information / open the official question page — or rephrase and browse related topics). Presentation reads **only** the canonical `AssistantAnswerType`; **the sensitivity predicate is not duplicated in UI code**, and no state claims a scholar reviewed the answer. **Temporary retrieval failure is a real state:** `answer()` throwing previously left the composer locked forever; it now produces a dedicated calm card that **fabricates no answer and claims no "no verified source" religious result**, keeps the question visible, unlocks the input, and offers **Try again** (which re-asks once and does not duplicate the question bubble). The unanswered question is marked non-persistable, so it never reaches disk. **Sensitive-history protection strengthened, not weakened:** the non-persistable id is now recorded **before** the await, so a failure mid-flight cannot leak the text. **Source rows:** actions render **only** when they have a working destination — a record with no `https` address and no article slug shows an honest "no openable address is recorded" line instead of a dead button, while its title and exact locator stay visible. Title and locator are wrapped in their **own** text direction, so a Latin citation stays LTR inside the Arabic page. **History:** day separators and per-message times from the existing `createdAt` via `MaterialLocalizations` (no new dependency, no new stored field); clearing keeps its confirmation and now shows a **distinct honest failure message** instead of staying silent — success is still reported only after a real delete. No account, sync or cloud-history wording added. **Accessibility:** keyboard-inset, 1.5x text on a 360 px screen in TR/EN/AR, Arabic RTL, live-region loading, and reduced motion (the spinner becomes a static mark). **Device-validation follow-up — "Zina haram mıdır?" read as a generic no-source failure.** Diagnosis found the **pipeline already correct**: normalized `zinaharammidir` → tokens `[zina, haram, midir]` → canonical class **`halalHaramVerdict`** → **sensitive → never persisted** → retrieval **strong** (`art-helal-ve-haram-nedir`) → `officialFatwaRequired`. **No published record can answer a ruling question** — `contentType: officialFatwa` is **zero** in all three locales — so the refusal branch is correct and was **not** weakened. Two real presentation defects caused the symptom: (1) the refusal copy **led with "…bilgi tabanımızda bulunmuyor"**, making it read identically to the no-source text — rewritten in TR/EN/AR to lead with the reason ("this is a ruling question; only the competent official authority issues a definite ruling"), refusal intact, no religious claim added, all three locales still distinct and Arabic-scripted; (2) the composer **dropped `sourceReferences`** on this path, so the genuinely relevant published explanation (İslam İlmihali 34. Baskı 2019, *Helaller ve Haramlar*, s. 383-384) appeared only as a bare title with **no citation** — it now carries its citation exactly as the `personalCase` branch already did, gated on `hasGrounded` so a weak match (the Arabic phrasing, `related`) still attaches **nothing**. `answerType`, `confidence: insufficient`, the safety notice and the official CTA are unchanged, and the sources block under any advisory state now carries the existing localized `assistantSourceNotDirectlyAddressing` line so no citation reads as grounding a ruling. **No hardcoded verdict, no new religious content, no invented source, no second classification policy in presentation** (the screen reads only the canonical `AssistantAnswerType`). Unrelated questions ("Blockchain teknolojisi nedir?") stay on the generic no-source path (asserted). **53 focused tests** (`flutter test test/features/assistant/task_095d_assistant_ux_test.dart`); Assistant suite **160/160**; full suite **1868 → 1921**; analyze clean; Functions untouched. Three merged assertions repaired, not weakened: two scroll tests now bring the suggestion into view before tapping (the taller landing card pushed it off a 360x560 viewport) and the verdict test now expects the **more specific** official-fatwa badge | branch `task/095d-assistant-ux-alpha-polish` | Samsung A36 visual validation, then merge |
| TASK 095C | IN PROGRESS — PR OPEN, NOT MERGED | Owner-assigned Profile quality (not a roadmap task). **Renumbered from TASK 096 to TASK 095C after device validation**, so the canonical roadmap **TASK 096 — Prayer calculation-method selection** keeps its number and stays PLANNED; nothing else was renumbered. The published branch name `task/096-functional-profile-mvp` is kept as-is — it is a branch label, not a task number | Functional Profile MVP. Profile home gains a **30-day plan overview computed only from stored `DailyPlan` records**: derived plan profile, day X of N, completed tasks with an `AppProgressBar`, fully-completed days, and a last-7-day window. New pure `ProfilePlanOverviewCalculator` finds the **contiguous block containing today** (a gap means a different plan period, so two periods are never merged), counts the recent window **backwards only** so future days are never called progress, and returns **`null` rather than zeros** when today has no plan — the screen then shows a calm explanation instead of a fake metric. No streak, score, level, badge, average or projection is introduced. **Plan preferences:** the existing personalization screen and its validation are reused unchanged; the new action is regeneration. `InitialDailyPlanOrchestrator` gains `regenerateFromToday()` — **the same generator, request validation and `CoreDailyPlanItemSource`**, so no second plan engine exists. It **never runs silently**: a dialog states exactly what happens, and cancelling calls nothing. It rewrites **only today→+29** (`savePlans` preserves days outside the batch, so **past days are untouched**), carries completion forward for items whose **stable item ID** exists in both plans, and reports `droppedCompletedItems` for tasks the new preferences removed — the consequence is stated before confirmation and again after. Failure **never reports success**: a read or write failure leaves the store unchanged and shows a distinct message. Duplicate taps are blocked twice — the button is disabled while running and the orchestrator memoizes the in-flight operation, so two calls produce **one** write. **Settings:** only rows with working destinations are shown (language, prayer/reminders, prayer history, Quran preferences and saved verses, Learn, content sources, privacy/local data, about, licenses); **theme/dark mode and a reduced-motion toggle are deliberately absent** because neither exists in the app — asserted by test rather than shipped as dead rows. Qibla stays in Prayer. **Account truthfulness:** a local-data card states that preferences, plan and progress live only on this device, that **cloud backup and account sync are not active**, and that uninstalling or clearing app data removes local progress; a test asserts **no tappable account/login/sync/backup affordance** exists and that any sentence mentioning cloud says it is not active. No login, Firebase sync or account creation was added. TR/EN/AR complete, Arabic RTL, 1.5x text on a 360 px screen, semantics and reduced motion covered. **34 focused tests** (`flutter test test/features/profile`; files `task_095c_*`); full suite **1834 → 1868**; analyze clean; Functions untouched | branch `task/096-functional-profile-mvp` | Samsung A36 visual validation, then merge |
| TASK 095A | IN PROGRESS — PR OPEN, NOT MERGED | Owner-assigned Quran quality (not a roadmap task; **TASK 096 and every later roadmap number are unchanged**) | Quran audio follow-along and near-seamless playback. **Root cause of the 1–2 s inter-verse gap, read from the package source rather than guessed:** `AudioPlayer.setClip` builds a **brand-new one-item playlist and re-runs `_load()` on every call**, so each verse boundary tore down and re-prepared the media source and re-filled the buffer. The old handler called `setClip` + `seek(0)` + `play()` per verse. **Fix — one managed queue:** new pure `QuranAudioQueue` builds the ordered clip list **before playback starts** (continuous = the whole chapter so previous/next both work; single verse = exactly one clip, so the player structurally cannot advance), and the new `QuranAudioQueuePlayer` port is handed the entire sequence in **one** `setQueue` call. The `just_audio` adapter maps it to `setAudioSources` of `ClippingAudioSource`s over the same MP3, so the next verse is prepared while the current one plays and the handler does **nothing** at a verse boundary — the active verse is *derived* from the player's reported queue index. Skip next/previous is now a queue seek, not a reload. Pause/resume/stop/error/interruption handling, the MediaItem metadata rules, notification controls and the recitation source are unchanged; **no dependency added**. **Honest limitation:** timings come verbatim from MP3Quran `ayat_timing` and are never trimmed or shifted — silence inside a verse's own range stays (trimming risks cutting recitation) and network buffering still applies on start/seek, so this is **not** claimed as true gapless. **Follow-along:** the reader highlights the playing verse (existing calm styling — no flashing, no Quran-text animation, no word movement) and scrolls it to a comfortable 0.28 viewport alignment, clear of the app bar and playback panel. A **manual drag suspends auto-follow** (programmatic scrolls have no `dragDetails`, so it never disables itself) and a small "Follow recitation" action restores it and returns to the playing verse; the suspension clears automatically when playback stops or the session ends. Pause preserves the active verse and resume continues from it; another chapter's session never highlights this one; an audio error leaves **no** verse highlighted. Semantics add "currently playing verse" alongside the existing `selected` state. New TR/EN/AR keys `quranFollowRecitation` and `quranSemanticsPlayingVerse`; RTL, 1.5x text on a 360 px screen and reduced motion all covered. **36 focused tests** (queue/handler 19, follow-along 17); Quran suite **89 → 125**; full suite **1798 → 1834**; analyze clean; Functions untouched | branch `task/095a-quran-audio-follow-along` | Samsung A36 audio validation, then merge |
| TASK 094A | IN PROGRESS — PR OPEN, NOT MERGED | CP11 follow-on (owner-assigned; **renumbered from TASK 095 to TASK 094A** because canonical **TASK 095 is Qibla** — Qibla and every later roadmap task keep their numbers) | Motion System Foundation. Extends the existing `AppMotion` token file with a **role** layer — `tap` 120ms, `selection` 180ms, `stateChange` 240ms, calm curves (no back/elastic/bounce), `pressedScale` 0.98 — plus one reduced-motion decision point (`isReduced`/`of`/`pressScaleOf`) that every surface asks instead of reading `MediaQuery` itself. Panel, page-transition and stagger tokens were **deliberately NOT added**: nothing in this task consumes them, and an unused token later reads as an approved design decision. Two small reusable widgets: `AppMotionSwitcher` (fade + 0.94→1 scale cross-fade) and `AppPressScale` (`Listener`-based, so it never enters the gesture arena and cannot swallow a tap). Applied to exactly three flows — **Today completion** (shared `AppCard` surface animates via `AnimatedContainer`; the check indicator cross-fades; no celebration, confetti, streak or score, and the existing saving lock still makes the card read-only so rapid taps cannot double-write), **Onboarding selection** (selection mark cross-fades at `selection`; label and description are **not** animated separately; navigation left at its existing fast default — no route-transition change), and **Quran audio** (`AppButton` cross-fades label↔spinner and `_VerseAudioAction` cross-fades listen/loading/pause; the lock is still immediate because `onPressed` is already `null` while loading, and the controller's existing `_busy` guard and error handling were **not touched**). **No Quran text moves or animates.** No dependency added, no application/persistence/religious-content change, no screen redesigned. Two merged assertions repaired, not weakened: the recovery-note "no animation" test now bans attention-seeking animation and allows only the shared card surface (proving the note adds none of its own), and a warm-visual card test now settles before measuring the **resting** surface. 23 focused tests (`test/shared/motion/task_094a_motion_system_test.dart`); today+quran+shared+profile **1025/1025**; full suite **1705 → 1728**; analyze clean; Functions untouched | branch `task/095-motion-system-foundation` (published branch label kept; the **task** is TASK 094A) | **Samsung A36 visual validation PASSED** — motion confirmed intentionally subtle, no blocking visual or functional issue |

## Selected forward milestones

| Task | Status | Checkpoint | Summary |
|---|---|---|---|
| TASK 092 | COMPLETED | CP11 | Official-answer / fatwa-source index foundation |
| TASK 093 | DEFERRED | CP11 | Assistant retrieval ranking and no-source UX — deferred until real official-answer records exist |
| TASK 094 | COMPLETED | CP11 | Learn/Assistant security, language and RTL checkpoint — F1, `editorialReview`, persistence-boundary hardening, F2 source drift, TR/EN/AR and RTL all closed |
| TASK 101 | PLANNED | CP12 | Closed alpha/beta package |
| TASK 115 | PLANNED | CP13 | Google Play soft launch — **first possible revenue** |
| TASK 121 | PLANNED | CP14 | First 30-day revenue review |
| TASK 122 | PLANNED | CP14 | **Commercial validation** and 5,000–10,000 TL target evaluation |
| TASK 130 | PLANNED | CP15 | App Store public launch (Android + iOS complete) |
| TASK 140 | PLANNED | CP16 | Khatm planner and V1.1 checkpoint |

## Open dependency PRs

| PR | Item | Status | Note |
|---|---|---|---|
| PR #4 | Drift 2.34.2 (+sqlite3 3.5.0) | DEFERRED | tied to TASK 066 blocker; TASK 075 adds: `drift_dev` capped at 2.34.0, so merging would desynchronise drift/drift_dev |
| PR unknown | `cloud_functions` 6.3.4 (Dependabot branch on origin) | **NEEDS TRIAGE** | found at TASK 075; not previously recorded; PR state UNVERIFIED (`gh` not installed on current machine) |
| — | `actions/checkout` v7 (Dependabot branch on origin) | DEFERRED | unchanged; major-version bump |
| PR #3 | flutter_local_notifications 22.1.0 | SUPERSEDED | applied by TASK 070C via PR #14 on validated baseline |
| PR #6 | fast-xml-parser 5.10.1 | SUPERSEDED | applied by TASK 069 on Node 22 baseline |
| PR #2 | flutter-action 2.23.0 | SUPERSEDED | closed; replaced by PR #8 (TASK 067) |

