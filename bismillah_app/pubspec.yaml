name: bismillah_app
description: "Bismillah — premium Islamic lifestyle companion. Flutter app scaffold and foundation."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  drift: ^2.34.1
  drift_flutter: ^0.3.0
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.4
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.1
flutter:
  uses-material-design: true
